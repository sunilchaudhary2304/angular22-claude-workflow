# Angular Component Patterns

Use this skill when creating or modifying standalone Angular components.

## Rules
- Prefer standalone components
- Use inject() for dependencies
- Keep templates simple and use Bootstrap utilities
- Add a component spec when new UI is introduced
